<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lucian.lucian_blog.dao.UserDao">
    <resultMap id="securityMap" type="com.lucian.lucian_blog.dto.SecurityDTO">
        <id column="u_id" property="id"/>
        <result column="nick_name" property="nickName"/>
        <result column="password" property="password"/>
        <result column="locked" property="locked"/>
        <result column="enabled" property="enabled" jdbcType="BOOLEAN"/>
        <result column="locked" property="locked" jdbcType="BOOLEAN"/>
        <collection property="roles" ofType="com.lucian.lucian_blog.dto.RoleDTO">
            <result column="role_name" property="name"/>
            <result column="role_zh_name" property="zhName"/>
        </collection>
    </resultMap>
    <select id="loadUserByUsername" resultMap="securityMap">
        select u.id as u_id, u.nick_name, u.password, u.locked, u.enabled, r.name as role_name,
               r.zh_name as role_zh_name
        from users u
                 left join user_roles ur on u.id = ur.user_id and ur.deleted_at is null
                 left join roles r on ur.role_id = r.id and r.deleted_at is null
    </select>
</mapper>